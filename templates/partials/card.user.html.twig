<div class="col-xl-4 col-lg-4 col-md-8 col-10 m-auto text-center">

    <div class="card bg-light my-5 shadow ">
        <h3 class="card-header">{{user.id}} - {{user.firstname}} {{user.lastname}}</h3>
        <img height="250px" width="250px" class="mx-auto rounded-circle border border-success m-2" src="{{user.picture}}" alt="Card image">
        <div class="card-body">
          <p class="card-text">{{user.presentation}}<br/><strong>{{user.articles | length}}</strong> articles !</p>
          <a href="{{path("user_show",{id:user.id})}}" class="btn btn-primary rounded"><i class="fa-sharp fa-solid fa-eye"></i></a>
          <a href="{{path("user_delete",{id:user.id})}}" onclick="return confirm('TU ES SÛR ???');" class="btn btn-danger rounded"><i class="fa-sharp fa-solid fa-trash"></i></a>
        </div>
        <div class="card-footer text-muted">
          {{user.email}}
        </div>
    </div>
    
</div>